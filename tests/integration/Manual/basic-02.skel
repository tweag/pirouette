---- MODULE BasicTwo ----
EXTENDS Naturals
VARIABLE st
CONSTANT MAXDEPTH

vars == << st >>

----
----

Init == st = St(Nil)

Spec == Init /\ []([Next]_vars) /\ WF_vars(Next)

Inv == (or(st.arg0) = False) ~> [](or(st.arg0) = True)
====
