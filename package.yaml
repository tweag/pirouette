verbatim:
  cabal-version: 2.4

name: pirouette
version: 0.0.0
extra-source-files:
  - LICENSE
  - README.md

dependencies:
  - base >= 4.9 && < 5
  - mtl >= 2.2.2
  - bytestring
  - containers
  - lens
  - text
  - prettyprinter
  - gitrev
  - optparse-applicative
  - parsec
  - megaparsec
  - uniplate
  - hspec
  - QuickCheck
  - interpolate
  - deriving-compat
  - simple-smt
  - process
  - tlaplus
  - tlaplus-qq
  - flat >= 0.4.5
  - plutus-core
  - list-t

library:
  source-dirs: src
  ghc-options:
    -Wall
    -Wno-orphans

executable:
  main: Main.hs
  source-dirs: executable
  dependencies:
    - pirouette
  ghc-options:
    -Wall
    -Wno-orphans

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - tests/unit
    dependencies:
      - pirouette
